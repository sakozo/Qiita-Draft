JQueryオブジェクトとはなんだ？となったので考えてみた

⚫︎オブジェクト指向について学んだことはあるけどなんとなくしかわからない
JQueryオブジェクト

⚫︎そもそもJQueryオブジェクトという言葉について
まず、頭のJQueryとはJavaScriptのコードを容易に記事できるようにするために設計されたJavaScriptライブラリである
ーなるほどJavaScriptが楽に書けるライブラリ（便利なプログラムのあつまり）なんだな

後半のオブジェクトとは
オブジェクト指向プログラミングにおいて、プログラム上の手続きの対象を抽象化する概念である
ーなるほどわからん、この記事ではオブジェクトとはプログラムを書く際に、
クラスに属性（プロパティ、情報、データ）と操作（メソッド、ふるまい）を書いたものとします

ピ◯チュウ
属性
体力:35
攻撃:55
防御:40

操作（技）
電気ショックメソッド
def 電気ショック
  相手に威力40の攻撃をする
  10%の確率で相手をマヒにする
end

なきごえメソッド
def なきごえ
  ピカピカとコンソールに出力する
end

どろぼうメソッド
def どろぼう
  相手の持ち物を奪ってくる
end

という感じです
ここで、思い出していただきたいことは
ピカチュウオブジェクト（インスタンス）があると
ピカチュウの情報である攻撃力を調べることも
ピカチュウのメソッドである電気ショックを使ってもらうこともできると言うことです
（①そのインスタンスの情報を調べる
②インスタンスのメソッドを呼び出す）2つの使い方が出来ます。


⚫︎JQueryオブジェクトの中を見てみる
JQueryオブジェクトとは$()のメソッドの戻り値として返ってくるものです。
$(.message)と書いてあるとこの部分は

ピカチュウオブジェクトには体力などの情報と、電気ショックなどの振る舞いを持っていました

JQueryの中身をconsole.log()で出力してみます



⚫︎（補足）戻り値がピンとこないときの説明
